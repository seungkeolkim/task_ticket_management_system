# 저장소 작업 지침

## Pull Request 작성

- PR 제목과 본문은 항상 한국어로 작성한다.
- 본문에는 실제 변경 동작, 검증 결과와 남아 있는 범위를 간결하게 기록한다.

## 매 세션 시작 시 반드시 확인할 문서

계획 수립, 파일 수정, 코드 검토 또는 구현 작업을 시작하기 전에 다음 문서를 확인한다.

1. `REQUIREMENTS.md`를 읽는다.
2. `IMPLEMENTATION_ROADMAP.md`를 읽는다.
3. `docs/decisions/README.md`와 해당 인덱스에 나열된 모든 Markdown 문서를 읽는다.
4. DB 관련 작업이라면 `docs/database_conventions.md`도 읽는다.
5. Python 코드 구현·수정 또는 운영 설정 작업이라면 `docs/logging_conventions.md`를 읽고 시스템 로그 규약을 준수한다.
6. 기존 구현을 참고하는 작업이라면 `REFERENCE_IMPLEMENTATION.md`를 읽고 저장소 루트의 `government-project-analysis-agent/` 로컬 clone을 우선 확인한다. 해당 clone이 있으면 같은 내용을 확인하기 위해 웹을 검색하지 않는다.

프로젝트 정책을 대화 기록에만 의존하지 않는다. 저장소에 있는 문서를 세션 간에 유지되는 공식 작업 맥락으로 사용한다.

## 시스템 로깅

- 감사 로그와 개발·운영 진단용 시스템 로그를 구분한다.
- 새 코드의 로그 레벨, 포맷, context, 예외 처리와 민감정보 제외 기준은 `docs/logging_conventions.md`를 따른다.
- 기능을 구현하거나 검토할 때 주요 시작·완료·실패 지점에 시스템 로그가 필요한지 함께 검토한다.
- 공통 로깅 구조를 변경하면 관련 결정 문서, 규약 문서와 테스트를 같은 변경 작업에서 갱신한다.

## 의사결정 기록

- 구현 중 새로운 정책, 규칙, 상충 관계, 기술적 선택 또는 범위 결정이 필요하면 같은 변경 작업에서 `docs/decisions/` 아래의 해당 유형 문서를 갱신한다.
- `docs/decisions/README.md`에 정의된 유형별 접두사와 다음 번호를 사용한다.
- 결정 날짜, 상태, 결정 내용과 짧은 근거 또는 영향을 기록한다.
- 같은 결정을 중복해서 추가하지 않고 기존 항목을 갱신한다. 다른 결정으로 대체된 항목은 상태를 `SUPERSEDED`로 변경하고 대체 결정 ID를 명시한다.
- 제품 요구사항은 `REQUIREMENTS.md`에 유지한다. 결정 문서에는 요구사항 전체를 복사하지 않고 선택한 해석이나 구현 방향을 설명한다.
- 아직 확정되지 않은 사항은 해당 문서의 `Open decisions` 절에 기록한다. 확정되면 해당 항목을 제거하고 정식 결정 항목을 추가한다.
- 결정 변경이 제품 요구사항에 영향을 준다면 `REQUIREMENTS.md`, 해당 결정 문서와 `IMPLEMENTATION_ROADMAP.md`를 함께 갱신한다.
- 각 문서는 하나의 주제에 집중하고 읽기 쉬운 길이를 유지한다. 한 유형의 문서가 약 150줄을 넘으면 세부 유형으로 분리하고 인덱스를 갱신한다.

## 진척도 관리

- 체크리스트 항목이 실제로 완료될 때마다 `IMPLEMENTATION_ROADMAP.md`를 갱신한다.
- 단계의 모든 필수 하위 항목과 완료 기준을 충족한 경우에만 상위 단계를 완료로 표시한다.
- 계획만 세웠거나 일부만 구현한 작업은 완료로 표시하지 않는다.
