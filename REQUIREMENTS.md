# 태스크·티켓 관리 시스템 요구사항

## 1. 문서 목적

Jira를 사용하지 않는 조직에서 사용자, 프로젝트, 티켓 및 칸반 보드를 간단히 관리할 수 있는 사내용 웹 시스템을 구축한다.

초기 버전은 SQLite와 Python을 기반으로 작게 시작하되, 서비스 규모가 커졌을 때 다른 관계형 데이터베이스와 검색 시스템으로 교체하거나 확장하기 쉬운 구조를 목표로 한다.

## 2. 기본 원칙과 실행 환경

### 2.1 기술 및 실행 환경

- 백엔드는 Python으로 구현한다.
- 초기 데이터베이스는 SQLite를 사용한다.
- ORM 또는 데이터 접근 계층으로 SQLAlchemy와 같은 다중 DB 지원 도구를 사용한다.
- 비즈니스 로직이 SQLite 전용 SQL이나 파일 경로에 직접 의존하지 않도록 계층을 분리한다.
- 개발 환경은 Windows, 실제 실행 환경은 macOS를 기준으로 한다.
- 애플리케이션 코드에서 경로를 조합할 때 문자열 연결이나 OS 전용 구분자를 사용하지 않고 반드시 `os.path.join`을 사용한다.
- Docker 컨테이너로 실행하며, 영속 데이터는 호스트 디렉터리를 컨테이너에 volume mount하여 보관한다.

### 2.2 데이터 이관성

아래 데이터를 하나의 마운트 루트 아래에 저장하여, 해당 디렉터리 전체를 복사하면 다른 호스트로 이관할 수 있어야 한다.

```text
<mounted-data>/
├── database/       # SQLite DB 및 관련 파일
├── attachments/    # 티켓 및 댓글 첨부파일
└── backups/        # 선택적 백업 파일
```

- 저장 위치는 환경 변수로 설정할 수 있어야 한다.
- 컨테이너 내부 기본 예시는 `/app/data`로 하되 코드에 고정하지 않는다.
- DB 연결 문자열도 환경 변수로 설정하여 향후 PostgreSQL, MySQL 등으로 변경할 수 있어야 한다.
- 첨부파일 메타데이터는 DB에 저장하고, 실제 파일은 마운트 디렉터리에 저장한다.
- 향후 오브젝트 스토리지로 전환할 수 있도록 첨부파일 저장소 인터페이스를 분리한다.

## 3. 사용자 및 조직 관리

### 3.1 사용자

- 로그인한 사용자만 시스템에 접근할 수 있다.
- 사용자 역할은 다음 두 종류이다.
  - 일반 사용자
  - 시스템 관리자
- 시스템 관리자는 사용자 및 조직을 생성, 조회, 수정, 비활성화할 수 있다.
- 사용자 삭제가 기존 티켓 이력을 훼손하지 않도록 물리 삭제보다 비활성화를 기본으로 한다.
- 사용자의 최소 정보는 다음과 같다.
  - 로그인 ID
  - 표시 이름
  - 이메일(선택 또는 조직 정책에 따라 필수)
  - 비밀번호 해시
  - 시스템 역할
  - 소속 조직
  - 활성 여부
  - 생성일시 및 수정일시

### 3.2 인증 및 비밀번호

- 비밀번호 원문이나 복호화 가능한 형태를 저장하지 않는다.
- Argon2id 또는 bcrypt 같은 검증된 알고리즘으로 salt를 포함한 단방향 해시를 저장한다.
- 초기 로그인 요청에 별도의 자체 암호화 프로토콜은 적용하지 않는다.
- 단, 운영 환경에서는 로그인 정보와 세션 보호를 위해 HTTPS/TLS 사용을 필수로 권장한다.
- 로그인 성공 후 세션 또는 토큰 방식으로 인증 상태를 유지한다.
- 비활성 사용자는 로그인할 수 없다.
- 최초 기동 시 시스템을 관리할 수 있는 초기 관리자 계정을 생성할 수 있어야 한다.
- 사용자 데이터가 하나도 없을 때만 환경 변수 또는 일회성 관리 명령으로 초기 관리자 정보를 입력받는다.
- 초기 관리자 생성은 멱등적으로 동작해야 하며, 이미 사용자가 존재하면 재기동 시 관리자 계정을 추가하거나 비밀번호를 덮어쓰지 않는다.
- 초기 비밀번호 원문은 로그에 출력하지 않으며 최초 로그인 후 비밀번호 변경을 강제한다.
- 관리자는 사용자의 비밀번호를 임시 비밀번호로 초기화할 수 있으며, 해당 사용자도 다음 로그인 시 비밀번호를 변경해야 한다.

### 3.3 조직

- 조직은 0개 이상의 하위 조직을 가질 수 있다.
- 각 조직은 하나의 상위 조직을 가질 수 있으며 최상위 조직은 상위 조직이 없다.
- 조직 이동 시 자기 자신 또는 자기 하위 조직을 상위 조직으로 지정할 수 없다.
- 조직의 최소 정보는 다음과 같다.
  - 조직명
  - 상위 조직
  - 설명
  - 활성 여부
  - 생성일시 및 수정일시

### 3.4 조직 체계 가져오기 및 내보내기

- 시스템 관리자는 전체 조직 계층을 JSON 파일로 내보낼 수 있다.
- JSON에는 스키마 버전, 조직 식별 키, 조직명, 설명, 활성 여부 및 하위 조직 구조를 포함한다.
- 사용자, 프로젝트, 프로젝트 권한 및 티켓 데이터는 조직 JSON에 포함하지 않는다.
- 시스템 관리자는 동일한 형식의 JSON 파일로 조직 계층을 가져올 수 있다.
- 가져오기 전에 파일 형식, 스키마 버전, 중복 식별 키, 중복 조직명 및 순환 구조를 검증하고 적용 예정 결과와 오류를 미리 보여준다.
- 초기 버전의 가져오기는 기존 조직의 삭제나 전체 교체를 수행하지 않고 새 조직 추가와 명시적인 식별 키 기반 갱신만 허용한다.
- 가져오기 전체는 하나의 트랜잭션으로 처리하여 일부 조직만 반영되는 상태가 발생하지 않게 한다.

## 4. 프로젝트

### 4.1 프로젝트 범위

- Jira의 프로젝트와 유사한 격리 단위로 프로젝트를 제공한다.
- 티켓, 티켓 관계, 댓글, 첨부파일, 목록 설정 및 보드는 모두 하나의 프로젝트에 종속된다.
- 사용자는 자신이 참여자로 등록된 프로젝트만 조회할 수 있다.
- 서로 다른 프로젝트의 데이터는 화면, API 및 검색 결과에 노출되지 않아야 한다.
- 모든 프로젝트 종속 리소스 접근 시 서버에서 프로젝트 권한을 검증한다.

### 4.2 프로젝트 역할

- 프로젝트 역할은 다음과 같다.
  - 프로젝트 관리자
  - 프로젝트 사용자
- 프로젝트 관리자는 다음 작업을 수행할 수 있다.
  - 프로젝트 정보 및 구성원 관리
  - 프로젝트 내 모든 티켓 생성 및 조회
  - 다른 사용자가 생성하거나 담당하는 티켓 수정
  - 허용된 상태 전이 수행
  - 댓글 및 첨부파일 관리
- 프로젝트 사용자는 기본적으로 다음 작업을 수행할 수 있다.
  - 접근 가능한 프로젝트의 티켓 조회
  - 티켓 생성
  - 자신이 생성했거나 담당한 티켓 수정
  - 댓글 및 첨부파일 등록
- 시스템 관리자는 운영 및 장애 대응을 위해 모든 프로젝트에 접근할 수 있도록 하되, 해당 접근은 감사 로그에 남기는 것을 권장한다.
- 세부 권한 정책은 구현 시 별도의 권한 매트릭스로 확정한다.

### 4.3 프로젝트 기본 정보

- 프로젝트명
- 프로젝트 키: 티켓 번호 생성에 사용하는 짧고 고유한 문자열(예: `DEV-123`)
- 설명
- 활성 여부
- 프로젝트 관리자 및 사용자 목록
- 생성일시 및 수정일시

## 5. 티켓

### 5.1 티켓 유형 및 계층

지원하는 티켓 유형은 다음 세 종류이다.

| 유형 | 허용되는 상위 티켓 | 필수 여부 |
|---|---|---|
| Epic | 없음 | 상위 티켓 없음 |
| Task | Epic | Epic 없이도 생성 가능 |
| Subtask | Task | 상위 Task 필수 |

- Task는 동일 프로젝트 내 다른 Epic 아래로 이동하거나 Epic 소속을 해제할 수 있다.
- Subtask는 동일 프로젝트 내 다른 Task 아래로 이동할 수 있다.
- 프로젝트가 다른 티켓 아래로는 이동할 수 없다.
- 계층 규칙에 어긋나는 이동 및 순환 참조는 허용하지 않는다.
- 상위 티켓 이동 시 하위 티켓의 프로젝트와 데이터 일관성이 유지되어야 한다.

### 5.2 티켓 필드

티켓은 최소한 다음 정보를 가진다.

- 프로젝트
- 프로젝트 내 고유 티켓 번호 및 표시 키
- 유형: Epic, Task, Subtask
- 제목
- 설명
- 상태
- 중요도
- 상위 티켓
- 생성자(creator)
- 담당자(handler/assignee, 미지정 가능)
- 생성일시
- 수정일시
- 마감일(due date, 선택)
- 완료일시(완료 상태 진입 시 기록 권장)
- 취소일시(취소 상태 진입 시 기록 권장)
- 정렬 및 보드 표시를 위한 순서 값

향후 필요 시 다음 필드를 추가할 수 있도록 확장 가능하게 설계한다.

- 시작 예정일
- 예상 작업량 또는 스토리 포인트
- 라벨
- 컴포넌트
- 실제 완료일
- 스프린트

### 5.3 중요도

초기 중요도는 다음 5단계를 권장한다.

| 중요도 | 의미 |
|---|---|
| Trivial | 업무 영향이 거의 없는 개선 또는 정리 |
| Minor | 영향이 작고 우회가 가능한 문제 |
| Major | 일반적인 기본 중요도 |
| Critical | 주요 기능 또는 일정에 큰 영향이 있는 문제 |
| Blocker | 다른 업무 진행이나 서비스를 막는 최우선 문제 |

- 기본값은 `Major`로 한다.
- 내부 저장값은 변경에 강한 영문 코드로 유지하고, 화면 표시명은 다국어 처리 가능한 형태로 분리한다.

### 5.4 상태 및 유한 상태 기계

초기 상태는 다음과 같다.

- 등록
- 진행중
- 완료
- 보류
- 취소

상태 변경은 유한 상태 기계(Finite State Machine, FSM)로 제한한다. 초기 전이 규칙은 다음과 같이 제안한다.

| 현재 상태 | 이동 가능한 상태 |
|---|---|
| 등록 | 진행중, 보류, 취소 |
| 진행중 | 완료, 보류, 취소 |
| 보류 | 등록, 진행중, 취소 |
| 완료 | 진행중 |
| 취소 | 등록 |

- 완료 및 취소 상태는 업무 흐름상 종료 상태로 취급하되, 재개를 위한 명시적 역전이를 허용한다.
- 상태 변경은 API에서도 반드시 검증하며 UI 제한만으로 처리하지 않는다.
- 상태 변경 시 변경자, 이전 상태, 새 상태, 변경일시를 이력에 기록한다.
- 향후 프로젝트별 워크플로 설정이 가능하도록 상태 및 전이 규칙을 도메인 서비스 또는 설정 계층으로 분리한다.

### 5.5 티켓 관계

초기 버전은 다음 관계를 지원한다.

- 관계 있음(Related)
- 의존함(Depends on)

요구사항은 다음과 같다.

- 하나의 티켓은 관계를 가지지 않거나 여러 관계를 가질 수 있다.
- 관계 수에는 별도 제한을 두지 않는다.
- 관계는 동일 프로젝트의 티켓 사이에서만 생성할 수 있다.
- `Related`는 대칭 관계로 취급한다.
- `Depends on`은 방향이 있는 관계로 취급한다. A와 B는 모두 티켓이며, 예를 들어 A가 B에 의존할 수 있다.
- 동일한 두 티켓 사이의 완전히 동일한 관계는 중복 생성하지 않는다.
- 티켓 자신과의 관계는 생성할 수 없다.
- 티켓 상세 화면에는 관계가 있는 티켓을 Jira와 유사한 작은 목록으로 표시한다.
- 관계 목록에는 관계 방향, 티켓 키, 제목 및 현재 상태를 표시하고 각 티켓의 상세 화면으로 이동할 수 있게 한다.
- A 화면에는 `B에 의존함`, B 화면에는 역방향 표현인 `A가 이 티켓에 의존함`으로 표시한다. 역방향 표현은 별도의 관계 유형으로 저장하지 않는다.
- A가 하나 이상의 티켓에 의존한다면 의존 대상이 모두 `완료` 상태여야만 A를 `완료` 상태로 변경할 수 있다.
- 의존 대상이 미완료이면 UI에서 원인을 안내하고 완료 동작을 비활성화하며, API와 칸반 드래그에서도 완료 전이를 거부한다.
- `Related` 관계는 상태 전이를 제한하지 않는다.

### 5.6 본문 및 댓글 포맷팅

- 티켓 설명과 댓글은 기본적인 서식 입력을 지원한다.
- 최소 지원 서식은 제목, 굵게, 기울임, 취소선, 목록, 인용, 코드 및 링크이다.
- 저장 포맷은 Markdown을 우선 권장한다.
- 뷰어는 저장된 포맷을 동일하게 렌더링한다.
- XSS 방지를 위해 렌더링 결과의 HTML을 서버 또는 클라이언트에서 검증된 방식으로 sanitize한다.
- 추후 에디터 교체가 가능하도록 저장 데이터와 특정 에디터 구현을 강하게 결합하지 않는다.

### 5.7 댓글

- 프로젝트 접근 권한이 있는 사용자는 권한 정책에 따라 댓글을 등록할 수 있다.
- 댓글은 작성자, 본문, 생성일시 및 수정일시를 가진다.
- 댓글 본문은 티켓 설명과 같은 포맷팅을 지원한다.
- 댓글 수정 및 삭제 정책은 작성자와 프로젝트 관리자 권한을 기준으로 한다.
- 삭제된 댓글의 감사 필요성을 고려하여 soft delete를 권장한다.
- 티켓 설명과 댓글에서 `@사용자` 형식의 멘션을 지원한다.
- 같은 프로젝트에 접근할 수 있는 사용자만 멘션 후보로 제공하고 서버에서도 대상 사용자의 프로젝트 접근 권한을 검증한다.
- 멘션이 생성되면 대상 사용자에게 미확인 멘션 항목을 생성한다.
- 멘션 수정 시 같은 사용자에게 중복 항목이 계속 생성되지 않도록 원본과 대상 사용자의 조합을 식별한다.
- 이메일, 실시간 push 및 toast 알림은 초기 범위에 포함하지 않는다.

### 5.8 첨부파일

- 티켓과 댓글에 파일을 첨부할 수 있다.
- 이미지 파일은 상세 뷰에서 미리보기로 표시한다.
- 이미지가 아닌 파일은 파일명, 크기 및 다운로드 링크를 표시한다.
- 파일명 충돌과 경로 조작을 방지하기 위해 내부 저장명은 UUID 등으로 생성한다.
- 원본 파일명, MIME type, 크기, 저장 키, 업로더 및 업로드 일시는 DB에 기록한다.
- 허용 확장자, MIME type 및 최대 파일 크기는 환경 설정으로 제한한다.
- 파일 제공 시 프로젝트 접근 권한을 검사해야 하며, 마운트 디렉터리를 웹 서버의 공개 정적 경로로 직접 노출하지 않는다.

### 5.9 변경 이력

Jira와 유사한 추적성을 위해 다음 변경을 티켓 활동 이력으로 저장하는 것을 권장한다.

- 상태 변경
- 담당자 변경
- 중요도 변경
- 상위 티켓 변경
- 제목 및 설명 변경
- 마감일 변경
- 티켓 관계 추가 및 삭제
- 첨부파일 추가 및 삭제

각 이력에는 변경자, 변경 시각, 변경 항목, 이전 값 및 새 값을 기록한다.

### 5.10 티켓 삭제 및 휴지통

- 티켓 삭제는 즉시 물리 삭제하지 않고 프로젝트 휴지통으로 이동하는 soft delete 방식으로 처리한다.
- 프로젝트 관리자와 시스템 관리자만 티켓을 휴지통으로 이동하거나 복구할 수 있다.
- 휴지통으로 이동한 티켓은 일반 목록, 칸반 보드 및 일반 사용자의 직접 접근에서 제외하고 수정할 수 없게 한다.
- Epic 또는 Task를 휴지통으로 이동하면 그 하위 Task와 Subtask도 하나의 단위로 함께 이동하며, 복구할 때도 함께 복구한다.
- 다른 활성 티켓이 삭제 대상 티켓에 `Depends on` 관계를 가지고 있으면 관계를 먼저 해제하거나 해당 티켓을 완료하기 전까지 휴지통 이동을 거부한다.
- 휴지통 화면에는 티켓 키, 제목, 유형, 삭제자, 삭제일시 및 영구 삭제 예정일을 표시한다.
- 휴지통에 들어간 지 30일이 지난 티켓은 정기 정리 작업을 통해 자동으로 영구 삭제한다.
- 영구 삭제 시 하위 티켓, 댓글, 관계, 활동 이력, 멘션 및 실제 첨부파일도 함께 정리한다.
- 삭제 후 복구하거나 영구 삭제해도 이미 발급한 티켓 번호는 다시 사용하지 않는다.

## 6. 내 작업 대시보드

- 로그인에 성공하면 내 작업 대시보드를 기본 화면으로 표시한다.
- 대시보드에는 다음 정보를 요약 카드와 짧은 티켓 목록으로 제공한다.
  - 내가 담당한 미완료 티켓
  - 내가 생성한 미완료 티켓
  - 마감일이 지난 티켓
  - 오늘 또는 이번 주에 마감되는 티켓
  - 최근 수정된 내 티켓
- 각 요약의 티켓 수를 표시하고, 항목을 선택하면 해당 조건이 적용된 전체 티켓 목록으로 이동한다.
- 사용자가 접근 가능한 프로젝트의 티켓만 집계하고 표시한다.
- 대시보드에 `미확인 멘션` 영역을 제공한다.
- 미확인 멘션에는 멘션한 사용자, 프로젝트, 티켓 키, 댓글 또는 설명의 일부 및 멘션 시각을 표시한다.
- 멘션을 선택하면 해당 티켓과 댓글 위치로 이동하며 확인 상태로 변경한다.
- 사용자는 개별 멘션 또는 모든 멘션을 확인 상태로 변경할 수 있다.
- 접근 권한을 잃은 프로젝트의 멘션은 내용이나 티켓 존재 여부를 노출하지 않는다.
- 대시보드 정보는 페이지 진입 및 수동 새로고침 시 갱신하며 실시간 push, toast 및 이메일 알림은 제공하지 않는다.

## 7. 티켓 목록

### 7.1 기본 동작

- Jira의 티켓 목록과 유사한 표 또는 목록 형태로 표시한다.
- 항목을 클릭하면 현재 목록 화면 안에서 상세 뷰어를 인라인 패널로 표시한다.
- `크게 보기`를 선택하면 독립된 티켓 상세 페이지를 연다.
- URL로 특정 티켓 상세 페이지에 직접 접근할 수 있어야 한다.
- 사용자가 접근할 수 없는 프로젝트 또는 티켓은 존재 여부도 불필요하게 노출하지 않는다.

### 7.2 필터 및 정렬

다음 항목으로 필터할 수 있다.

- 티켓 유형
- 상태
- 중요도
- Epic
- 상위 Task
- 생성일 및 수정일 범위
- 마감일 범위
- 생성자
- 담당자
- 담당자 미지정 여부

다음 항목을 기준으로 정렬할 수 있도록 권장한다.

- 생성일
- 수정일
- 마감일
- 중요도
- 티켓 번호

- 여러 필터를 동시에 적용할 수 있어야 한다.
- 필터 상태는 URL query parameter로 표현하여 새로고침 및 링크 공유가 가능하도록 권장한다.

### 7.3 페이지네이션

- 서버 사이드 페이지네이션을 지원한다.
- 페이지 크기는 사용자가 10개, 20개, 50개 중 선택할 수 있다.
- 기본 페이지 크기는 20개를 권장한다.
- 안정적인 페이지 결과를 위해 항상 명시적인 보조 정렬 기준을 적용한다.

### 7.4 텍스트 검색 확장 계획

- 초기 MVP에서는 제목 및 제한적인 필드에 대한 단순 검색을 제공하거나 검색 기능을 제외할 수 있다.
- 향후 제목, 설명 및 댓글을 대상으로 한국어 전문 검색을 지원한다.
- 검색 로직은 저장소 계층 또는 검색 서비스 인터페이스 뒤에 분리하여 DB 쿼리와 직접 결합하지 않는다.
- SQLite FTS만으로 한국어 형태소 검색 품질이 부족할 수 있으므로 규모와 품질 요구가 생기는 시점에 PostgreSQL 전문 검색, PGroonga, OpenSearch/Elasticsearch 등의 도입을 검토한다.
- 검색 인덱스는 원본 데이터가 아니라 재생성 가능한 파생 데이터로 취급한다.
- 모든 검색 결과에는 프로젝트 접근 권한 필터가 적용되어야 한다.

### 7.5 저장된 필터

- 사용자는 현재 선택한 필터, 정렬 기준 및 표시 개수를 이름을 붙여 개인 필터로 저장할 수 있다.
- 프로젝트 관리자는 프로젝트 구성원이 사용할 수 있는 공유 필터를 생성, 수정 및 삭제할 수 있다.
- 저장된 필터는 티켓 목록과 호환되는 범위에서 칸반 보드에서도 불러올 수 있다.
- 초기 버전에는 JQL과 같은 사용자 입력형 쿼리 언어를 도입하지 않는다.
- 저장 데이터는 허용된 필드, 연산자, 값, 정렬 및 페이지 크기로 구성된 버전이 명시된 JSON 구조를 사용한다.
- 저장된 JSON은 실행 가능한 SQL이나 코드가 아니며, 불러올 때 서버가 필드, 타입, 프로젝트 권한을 다시 검증한 뒤 기존 목록 조회 조건으로 변환한다.
- 저장된 필터의 이름 변경, 덮어쓰기 및 삭제를 지원한다.

## 8. 칸반 보드

### 8.1 기본 구성

- 칸반 보드는 프로젝트에 종속된다.
- 상태별 열에 Task와 Subtask를 카드 형태로 표시한다.
- Epic은 카드의 상위 묶음으로 표현하며 Epic 단위로 접거나 펼칠 수 있다.
- Epic이 없는 Task를 위한 별도 `Epic 없음` 묶음을 제공한다.
- 카드를 클릭하면 티켓 상세 뷰어 또는 상세 페이지로 이동한다.

### 8.2 계층 표시 규칙

- Task와 그 Subtask가 같은 상태인 경우 Subtask를 해당 Task 아래에 달린 형태로 표시한다.
- Subtask가 상위 Task와 다른 상태에 있는 경우 해당 Subtask를 자신의 상태 열에 표시한다.
- 다른 상태 열에 표시된 동일 상위 Task의 Subtask들은 상위 Task를 식별할 수 있는 하나의 그룹으로 묶는다.
- 카드에는 최소한 티켓 키, 제목, 중요도, 담당자 및 마감일을 표시한다.

### 8.3 드래그를 통한 상태 변경

- 사용자는 카드를 드래그하여 다른 상태 열로 이동할 수 있다.
- FSM에서 허용한 상태로만 드롭할 수 있다.
- 허용되지 않은 대상은 UI에서 명확히 표시하고 드롭할 수 없게 한다.
- 서버는 드래그 요청에 대해서도 현재 상태와 권한 및 FSM 전이를 다시 검증한다.
- 미완료 티켓에 의존하는 카드의 `완료` 상태 이동은 드롭할 수 없게 표시하고 서버에서도 거부한다.
- 동시 수정 충돌 시 사용자에게 갱신 필요를 알리고 보드를 새 데이터로 동기화한다.

### 8.4 필터

티켓 목록과 동일한 주요 필터를 지원한다.

- 유형
- 상태
- 중요도
- Epic
- 생성자
- 담당자
- 날짜 범위
- 향후 Sprint

## 9. Sprint 확장 계획

Sprint는 초기 MVP 범위에서는 제외하되, 다음 기능을 추가할 수 있도록 데이터 모델과 서비스 경계를 준비한다.

- 프로젝트별 Sprint 생성
- 시작일 및 종료일 지정
- 계획, 진행중, 종료 상태 관리
- Sprint 시작 및 종료 동작
- 다수의 미완료 Ticket 및 Epic 일괄 등록
- Sprint 종료 시 미완료 티켓을 백로그 또는 다음 Sprint로 일괄 이동
- 티켓 목록 및 칸반 보드에서 Sprint 필터 제공
- Sprint별 완료율 및 기본 통계 제공

Sprint와 Ticket 사이의 관계는 향후 이력 요구를 고려한다. 티켓의 현재 Sprint만 필요한지, 여러 Sprint에 걸친 이동 이력을 보존할지 구현 전에 확정한다.

## 10. 데이터 및 애플리케이션 설계 지침

### 10.1 계층 분리

권장 계층은 다음과 같다.

```text
API / Web UI
    ↓
Application Service
    ↓
Domain / Permission / FSM
    ↓
Repository Interface
    ↓
SQLAlchemy ORM / File Storage / Search Adapter
```

- API 계층에서 직접 ORM 쿼리를 작성하지 않는다.
- 프로젝트 권한, 티켓 계층 규칙 및 FSM 검증은 재사용 가능한 서비스로 구현한다.
- DB별 차이가 필요한 기능은 저장소 또는 adapter 안에 격리한다.
- SQLite에서는 외래 키 검사를 명시적으로 활성화한다.
- 스키마 변경은 Alembic과 같은 migration 도구로 관리한다.
- 날짜와 시간은 DB에 UTC로 저장하고 화면에서 사용자 또는 시스템 시간대로 변환한다.

### 10.2 동시성 및 무결성

- 프로젝트 키 및 프로젝트 내 티켓 번호에는 고유 제약 조건을 적용한다.
- 티켓 관계, 프로젝트 구성원 등 중복되면 안 되는 데이터에는 DB 제약 조건을 함께 둔다.
- 티켓 수정 충돌 방지를 위해 버전 번호 또는 수정일시 기반 optimistic locking을 권장한다.
- 상위 티켓 변경, 상태 변경 및 관련 이력 기록은 하나의 트랜잭션으로 처리한다.

### 10.3 API 및 오류 처리

- UI와 분리된 API 구조를 권장한다.
- 모든 쓰기 요청은 인증, 프로젝트 권한, 입력값 및 도메인 규칙을 서버에서 검증한다.
- 오류 응답은 사용자 메시지와 개발용 오류 코드를 구분한다.
- 목록 API는 필터, 정렬, 페이지 및 페이지 크기를 명시적으로 입력받는다.

## 11. 보안 및 운영 요구사항

- 비밀번호 및 비밀 키를 코드나 이미지에 포함하지 않고 환경 변수 또는 secret으로 주입한다.
- 세션 쿠키 사용 시 `HttpOnly`, `Secure`, 적절한 `SameSite` 설정을 적용한다.
- 입력값 검증, CSRF 방어, XSS 방어 및 SQL injection 방어를 적용한다.
- ORM 사용 여부와 무관하게 동적 SQL 문자열 결합을 피한다.
- 첨부파일 업로드 시 파일 크기, MIME type 및 저장 경로를 검증한다.
- 로그인 실패 횟수 제한 또는 지연 정책을 권장한다.
- 사용자, 권한, 프로젝트 구성원 및 티켓 주요 변경에 대한 감사 로그를 권장한다.
- 컨테이너 상태 확인을 위한 health check endpoint를 제공한다.
- SQLite DB 및 첨부파일을 일관된 시점에 백업하고 복구 절차를 문서화한다.

### 11.1 백업 아카이브 다운로드(후속 기능)

- 시스템 관리자는 관리 화면에서 현재 영속 데이터를 하나의 압축 파일로 생성하여 다운로드할 수 있다.
- 실행 중인 SQLite 파일을 그대로 압축하지 않고 SQLite backup API 등을 이용해 일관된 DB 스냅샷을 먼저 만든다.
- 생성된 아카이브에는 DB 스냅샷, 첨부파일 및 백업 시각과 애플리케이션 버전을 담은 manifest를 포함한다.
- 기존 `backups` 디렉터리와 생성 중인 임시 파일은 아카이브에서 제외하여 백업 파일이 재귀적으로 포함되지 않게 한다.
- 백업 생성과 다운로드는 시스템 관리자만 수행할 수 있으며 작업 시작, 성공, 실패 및 다운로드 이력을 감사 로그에 기록한다.
- 다운로드용 파일은 설정된 보관 기간이 지나면 자동으로 삭제한다.
- 다른 DB로 전환할 가능성을 고려하여 DB 스냅샷 생성은 DB별 backup adapter로 분리한다.
- 초기 복구는 컨테이너를 중지한 상태에서 아카이브를 마운트 디렉터리에 복원하는 운영 절차로 제공하고, 웹 화면의 자동 복구는 별도 후속 기능으로 둔다.

### 11.2 UI 지원 범위

- 회사 정책상 모바일 접속을 지원하지 않으므로 모바일 전용 화면과 반응형 모바일 최적화는 현재 범위에서 제외한다.
- 다크 모드는 초기 범위에서 제외한다.
- 추후 테마 추가 비용을 낮추기 위해 화면 색상을 개별 컴포넌트에 하드코딩하지 않고 공통 CSS 변수 또는 디자인 토큰으로 관리한다.

## 12. 초기 MVP 범위

### 12.1 포함

- 로그인 및 로그아웃
- 최초 기동 시 초기 시스템 관리자 생성 및 비밀번호 초기화
- 일반 사용자 및 시스템 관리자 관리
- 계층형 조직 관리
- 조직 체계 JSON 가져오기 및 내보내기
- 프로젝트 생성 및 구성원/역할 관리
- Epic, Task, Subtask 생성·조회·수정 및 계층 이동
- 티켓 관계(Related, Depends on) 목록 표시 및 의존성 기반 완료 차단
- Markdown 기반 설명 및 댓글
- 사용자 멘션 및 대시보드의 미확인 멘션 확인
- 첨부파일 업로드·다운로드 및 이미지 표시
- FSM 기반 상태 변경
- 중요도, 생성자, 담당자, 날짜 관리
- 티켓 휴지통, 복구 및 30일 후 자동 영구 삭제
- 내 작업 대시보드
- 티켓 목록, 필터, 정렬 및 페이지네이션
- JQL 없는 개인·공유 저장 필터
- 인라인 상세 뷰 및 전체 상세 페이지
- Epic 그룹 기반 칸반 보드
- 드래그를 통한 상태 변경
- Docker 실행 및 volume mount 기반 영속화
- Alembic 기반 DB migration

### 12.2 후속 범위

- Sprint 및 백로그
- 한국어 전문 검색
- 관리자용 백업 아카이브 생성 및 다운로드
- 사용자 정의 상태와 워크플로
- 사용자 정의 필드
- 프로젝트 및 업무 통계
- 외부 인증(SSO, OAuth/OIDC, LDAP)
- 오브젝트 스토리지 기반 첨부파일 저장
- 필요성이 생길 경우 다크 모드

### 12.3 명시적 제외 범위

- 이메일 알림
- 실시간 push 및 toast 알림
- JQL과 같은 사용자 입력형 쿼리 언어
- 티켓 일괄 수정
- 티켓 복제
- 프로젝트 및 티켓 CSV 가져오기·내보내기
- 사용자 그룹 또는 팀 기반 권한 일괄 부여
- 모바일 접속 및 모바일 전용 UI

## 13. 구현 전 확정이 필요한 정책

다음 항목은 구현 착수 전에 최종 결정이 필요하다.

1. 프로젝트 사용자가 수정할 수 있는 티켓 범위
2. 댓글 및 첨부파일의 삭제 권한과 보존 정책
3. 완료 또는 취소 티켓의 수정 가능 범위
4. Epic 상태와 하위 Task 상태 사이의 자동 연동 여부
5. Task 이동 시 Subtask를 함께 처리하는 상세 정책
6. 조직 소속이 프로젝트 접근 권한에 자동으로 영향을 주는지 여부
7. 첨부파일별 최대 크기, 전체 저장 용량 및 허용 파일 형식
8. 세션 방식과 토큰 방식 중 초기 인증 방식
9. 감사 로그의 보존 기간
10. 초기 단순 텍스트 검색을 MVP에 포함할지 여부

## 14. 용어

| 용어 | 의미 |
|---|---|
| 시스템 관리자 | 전체 사용자, 조직 및 시스템을 관리하는 사용자 |
| 프로젝트 관리자 | 특정 프로젝트와 그 안의 티켓 및 구성원을 관리하는 사용자 |
| 프로젝트 사용자 | 특정 프로젝트를 이용하는 일반 참여자 |
| Creator | 티켓을 최초 등록한 사용자 |
| Handler / Assignee | 티켓을 현재 담당하는 사용자 |
| Parent Ticket | Task의 Epic 또는 Subtask의 Task |
| FSM | 허용된 상태 이동만 수행하도록 제한하는 유한 상태 기계 |
