# 태스크·티켓 관리 시스템 요구사항

## 1. 문서 목적

Jira를 사용하지 않는 조직에서 사용자, 프로젝트, 티켓 및 칸반 보드를 간단히 관리할 수 있는 사내용 웹 시스템을 구축한다.

초기 버전은 SQLite와 Python을 기반으로 작게 시작하되, 서비스 규모가 커졌을 때 다른 관계형 데이터베이스와 검색 시스템으로 교체하거나 확장하기 쉬운 구조를 목표로 한다.

## 2. 기본 원칙과 실행 환경

### 2.1 기술 및 실행 환경

- 백엔드는 Python으로 구현한다.
- 초기 데이터베이스는 SQLite를 사용한다.
- ORM 또는 데이터 접근 계층으로 SQLAlchemy와 같은 다중 DB 지원 도구를 사용한다.
- 비즈니스 로직이 SQLite 전용 SQL이나 파일 경로에 직접 의존하지 않도록 계층을 분리한다.
- 개발 환경은 Windows, 실제 실행 환경은 macOS를 기준으로 한다.
- 애플리케이션 코드에서 경로를 조합할 때 문자열 연결이나 OS 전용 구분자를 사용하지 않고 반드시 `os.path.join`을 사용한다.
- Docker 컨테이너로 실행하며, 영속 데이터는 호스트 디렉터리를 컨테이너에 volume mount하여 보관한다.

### 2.2 데이터 이관성

운영 설정과 영속 데이터는 각각 독립된 호스트 경로에 저장하고 컨테이너에도 별도로 마운트한다. 다른 호스트로 이관할 때는 아래 `config`와 `data` 디렉터리를 함께 복사할 수 있어야 한다.

```text
<project-or-deployment-root>/
├── config/
│   └── application.toml  # 외부 애플리케이션 설정 파일
└── data/
    ├── database/         # SQLite DB 및 관련 파일
    ├── attachments/      # 티켓 및 댓글 첨부파일
    ├── backups/          # 선택적 백업 파일
    └── logs/             # 시스템 진단 로그
```

- 설정 파일과 데이터 저장 위치는 각각 환경 변수로 변경할 수 있어야 한다.
- 컨테이너 내부 기본 예시는 설정 파일 `/app/config/application.toml`, 데이터 루트 `/app/data`로 하되 애플리케이션 코드에 고정하지 않는다.
- DB 연결 문자열도 환경 변수로 설정하여 향후 PostgreSQL, MySQL 등으로 변경할 수 있어야 한다.
- 첨부파일 메타데이터는 DB에 저장하고, 실제 파일은 마운트 디렉터리에 저장한다.
- 향후 오브젝트 스토리지로 전환할 수 있도록 첨부파일 저장소 인터페이스를 분리한다.

### 2.3 외부 설정

- 운영 중 조정할 수 있는 각종 설정값은 소스코드에 하드코딩하지 않고 외부 설정 파일에서 읽는다.
- 호스트의 기본 설정 파일은 최상위 `config/application.toml`을 사용하고 컨테이너에는 데이터 디렉터리와 별도로 마운트한다. 설정 파일 경로는 `APP_CONFIG_FILE` 환경 변수로 변경할 수 있다.
- Docker Compose 기동 시 `[server].port`를 호스트 공개 포트, 컨테이너 수신 포트와 health check에 동일하게 적용한다. Compose가 TOML을 직접 해석하지 못하므로 제공된 실행 래퍼를 표준 기동·중지 경로로 사용하며, 래퍼를 우회한 기동은 고정 기본 포트로 조용히 대체하지 않고 명확히 실패한다.
- POSIX 셸의 `run_compose.sh`와 Windows PowerShell의 `run_compose.ps1`에서 동일한 `start`·`stop` 명령과 공통 포트 검증기를 사용한다.
- 설정 우선순위는 `환경 변수 → 외부 설정 파일 → 애플리케이션 기본값`으로 한다.
- 설정 파일은 기동 시 읽고 검증하며, 변경된 설정은 애플리케이션 재시작 후 적용한다.
- 필수 설정이 없거나 형식과 범위가 잘못된 경우 안전한 기본값으로 조용히 대체하지 않고, 명확한 오류를 남기고 기동을 중단한다.
- 첨부파일 제한, 감사 로그 보존 기간과 정리 실행 주기, 휴지통 보존 기간, 세션 만료 시간, 백업 보관 정책, 페이지 크기 등 운영값을 외부 설정으로 제공한다.
- 비밀번호, 세션 서명 키 등의 비밀값은 일반 설정 파일에 저장하지 않고 환경 변수 또는 컨테이너 secret으로만 주입한다.

## 3. 사용자 및 조직 관리

### 3.1 사용자

- 로그인한 사용자만 시스템에 접근할 수 있다.
- 사용자 역할은 다음 두 종류이다.
  - 일반 사용자
  - 시스템 관리자
- 시스템 관리자는 사용자 및 조직을 생성, 조회, 수정, 비활성화할 수 있다.
- 사용자·조직 관리의 첫 연결 범위는 실제 조회와 생성이다. 신규 사용자는 활성 조직에 연결하고 임시 비밀번호의 첫 로그인 변경을 강제한다. 수정·비활성화·비밀번호 초기화와 조직 JSON 입출력은 후속 화면 확장에서 연결한다.
- 사용자 이메일은 선택이며 빈 값은 미등록으로 저장한다. 등록한 이메일은 앞뒤 공백 제거·소문자 정규화 후 고유하게 유지하고 기본 주소 형식을 검사한다.
- 사용자 삭제가 기존 티켓 이력을 훼손하지 않도록 물리 삭제보다 비활성화를 기본으로 한다.
- 사용자의 최소 정보는 다음과 같다.
  - 로그인 ID
  - 표시 이름
  - 이메일(선택 또는 조직 정책에 따라 필수)
  - 비밀번호 해시
  - 시스템 역할
  - 소속 조직
  - 활성 여부
  - 생성일시 및 수정일시

### 3.2 인증 및 비밀번호

- 비밀번호 원문이나 복호화 가능한 형태를 저장하지 않는다.
- Argon2id 또는 bcrypt 같은 검증된 알고리즘으로 salt를 포함한 단방향 해시를 저장한다.
- 초기 로그인 요청에 별도의 자체 암호화 프로토콜은 적용하지 않는다.
- 단, 운영 환경에서는 로그인 정보와 세션 보호를 위해 HTTPS/TLS 사용을 필수로 권장한다.
- 초기 버전은 서버 측 세션과 추측하기 어려운 opaque 세션 토큰 쿠키로 인증 상태를 유지한다.
- 비활성 사용자는 로그인할 수 없다.
- `/`는 내 작업 화면 주소로 유지하고, 미인증 웹 접근은 `/login?next=...`로 이동한다. 로그인 후 원래 내부 페이지로 복귀하되 임시 비밀번호 사용자는 비밀번호 변경을 먼저 완료한다.
- 비밀번호 변경은 기존 모든 세션을 무효화하고 재로그인을 요구한다. 로그아웃과 비밀번호 변경은 CSRF를 검증하는 POST 요청으로만 수행한다.
- 초기 로그인 ID는 공백 제거·소문자 정규화 후 영문 소문자·숫자·점·밑줄·하이픈의 3~100자이며 첫 글자는 영문 또는 숫자다. 비밀번호는 12~128자로 하고 공백만 있거나 제어문자가 포함된 값은 허용하지 않는다.
- 로그인 실패는 기본 15분 동안 ID별 5회 또는 접속 IP별 30회에 도달하면 제한하며 제한값은 외부 설정으로 조정한다.
- 최초 기동 시 시스템을 관리할 수 있는 초기 관리자 계정을 생성할 수 있어야 한다.
- 사용자 데이터가 하나도 없을 때만 환경 변수 또는 일회성 관리 명령으로 초기 관리자 정보를 입력받는다.
- 초기 관리자 생성은 멱등적으로 동작해야 하며, 이미 사용자가 존재하면 재기동 시 관리자 계정을 추가하거나 비밀번호를 덮어쓰지 않는다.
- 초기 관리자는 설정된 초기 조직에 연결한다. 기본 조직 key와 이름은 `default`와 `기본 조직`이며 같은 key의 활성 조직이 있으면 재사용하고 없으면 생성한다.
- 초기 비밀번호 원문은 로그에 출력하지 않으며 최초 로그인 후 비밀번호 변경을 강제한다.
- 관리자는 사용자의 비밀번호를 임시 비밀번호로 초기화할 수 있으며, 해당 사용자도 다음 로그인 시 비밀번호를 변경해야 한다.

### 3.3 조직

- 조직은 0개 이상의 하위 조직을 가질 수 있다.
- 각 조직은 하나의 상위 조직을 가질 수 있으며 최상위 조직은 상위 조직이 없다.
- 조직 이동 시 자기 자신 또는 자기 하위 조직을 상위 조직으로 지정할 수 없다.
- 새 조직의 안정 식별 키는 서버에서 생성하며, 조직명 앞뒤 공백을 제거하고 같은 부모 아래의 동일 이름을 거부한다(최상위 포함). 비활성 조직 또는 그 하위에는 신규 조직과 사용자를 등록할 수 없다.
- 조직의 최소 정보는 다음과 같다.
  - 조직명
  - 상위 조직
  - 설명
  - 활성 여부
  - 생성일시 및 수정일시

### 3.4 조직 체계 가져오기 및 내보내기

- 시스템 관리자는 전체 조직 계층을 JSON 파일로 내보낼 수 있다.
- JSON에는 스키마 버전, 조직 식별 키, 조직명, 설명, 활성 여부 및 하위 조직 구조를 포함한다.
- 사용자, 프로젝트, 프로젝트 권한 및 티켓 데이터는 조직 JSON에 포함하지 않는다.
- 시스템 관리자는 동일한 형식의 JSON 파일로 조직 계층을 가져올 수 있다.
- 가져오기 전에 파일 형식, 스키마 버전, 중복 식별 키, 중복 조직명 및 순환 구조를 검증하고 적용 예정 결과와 오류를 미리 보여준다.
- 초기 버전의 가져오기는 기존 조직의 삭제나 전체 교체를 수행하지 않고 새 조직 추가와 명시적인 식별 키 기반 갱신만 허용한다.
- 가져오기 전체는 하나의 트랜잭션으로 처리하여 일부 조직만 반영되는 상태가 발생하지 않게 한다.

## 4. 프로젝트

### 4.1 프로젝트 범위

- Jira의 프로젝트와 유사한 격리 단위로 프로젝트를 제공한다.
- 티켓, 티켓 관계, 댓글, 첨부파일, 목록 설정 및 보드는 모두 하나의 프로젝트에 종속된다.
- 사용자는 자신이 참여자로 등록된 프로젝트만 조회할 수 있다.
- 서로 다른 프로젝트의 데이터는 화면, API 및 검색 결과에 노출되지 않아야 한다.
- 모든 프로젝트 종속 리소스 접근 시 서버에서 프로젝트 권한을 검증한다.

### 4.2 프로젝트 역할

- 프로젝트 역할은 다음과 같다.
  - 프로젝트 게스트 (`PROJECT_GUEST`)
  - 프로젝트 사용자 (`PROJECT_USER`)
  - 프로젝트 관리자 (`PROJECT_ADMIN`)
- 프로젝트 게스트는 프로젝트와 그 안의 티켓·댓글·관계·변경 이력·첨부파일을 읽을 수 있지만 쓰기 작업은 수행할 수 없다. 프로젝트 게스트는 티켓 담당자로 지정하지 않는다.
- 프로젝트 사용자는 프로젝트 안에서 creator·assignee와 무관하게 모든 티켓 업무를 수행할 수 있다. 티켓 생성·수정·상태 전이·관계 관리, 댓글·첨부파일 관리와 휴지통 이동·복구를 포함한다.
- 프로젝트 관리자는 프로젝트 사용자의 모든 권한에 더해 프로젝트 기본 정보·활성 상태와 프로젝트 참여자·역할을 관리한다.
- 시스템 관리자는 운영 및 장애 대응을 위해 모든 프로젝트에 접근할 수 있도록 하되, 해당 override 접근은 반드시 감사 로그에 남긴다.
- 시스템 관리자는 사용자·조직과 전체 프로젝트를 관리하고 보존·정리 작업을 설정하거나 수동 실행할 수 있다. 자동 작업은 별도 사용자 역할을 만들지 않고 내부 system actor로 실행하며 관리자 수동 실행과 구분해 감사·시스템 로그에 남긴다.
- 권한은 프로젝트 역할 단위로 판정하고 creator·assignee에 따른 티켓별 수정 권한은 두지 않는다. FSM, 종료 티켓 잠금, 계층·의존성 규칙과 optimistic locking은 권한과 별개의 업무 규칙으로 동일하게 적용한다.

### 4.3 프로젝트 기본 정보

- 프로젝트명
- 프로젝트 키: 티켓 번호 생성에 사용하는 짧고 고유한 문자열(예: `DEV-123`)
- 프로젝트 키는 생성 후 변경하지 않으며 이미 발급한 티켓 표시 키도 유지한다.
- 프로젝트 키 입력은 앞뒤 공백 제거·대문자 정규화 후 영문자로 시작하는 영문·숫자 2~32자로 제한한다.
- 시스템 관리자는 프로젝트 생성 시 활성 사용자 한 명을 최초 프로젝트 관리자로 지정한다. 생성자를 자동 참여자로 등록하지 않는다.
- 첫 화면 연결 범위는 프로젝트 생성·조회, 참여자 추가와 내 프로젝트다. 프로젝트 수정·비활성화 및 기존 참여자의 역할 변경·제거는 후속 화면 확장에서 연결한다.
- 내 프로젝트는 시스템 관리자도 명시적으로 참여한 프로젝트만 보여주며, 시스템 관리자의 전체 프로젝트 조회는 별도 관리 화면에서 제공한다.
- 프로젝트 관리자와 시스템 관리자는 조직에 관계없이 활성 사용자를 프로젝트 게스트·사용자·관리자로 추가하고 기존 역할을 변경하거나 참여에서 제거할 수 있다. 후보 조회는 로그인 ID·표시 이름 검색과 최대 50개 결과를 제공하고 계정 비밀정보는 포함하지 않는다. 마지막 프로젝트 관리자는 제거하거나 하위 역할로 변경할 수 없다.
- 비활성 프로젝트는 기존 권한으로 조회할 수 있으나 새로운 참여자 등록과 티켓 생성·업무 필드 편집·상태 전이는 허용하지 않는다.
- 설명
- 활성 여부
- 프로젝트 게스트·사용자·관리자 목록
- 생성일시 및 수정일시

## 5. 티켓

### 5.1 티켓 유형 및 계층

지원하는 티켓 유형은 다음 세 종류이다.

| 유형 | 허용되는 상위 티켓 | 필수 여부 |
|---|---|---|
| Epic | 없음 | 상위 티켓 없음 |
| Task | Epic | Epic 없이도 생성 가능 |
| Subtask | Task | 상위 Task 필수 |

- Task는 동일 프로젝트 내 다른 Epic 아래로 이동하거나 Epic 소속을 해제할 수 있다.
- Subtask는 동일 프로젝트 내 다른 Task 아래로 이동할 수 있다.
- 프로젝트가 다른 티켓 아래로는 이동할 수 없다.
- 계층 규칙에 어긋나는 이동 및 순환 참조는 허용하지 않는다.
- 상위 티켓 이동 시 하위 티켓의 프로젝트와 데이터 일관성이 유지되어야 한다.

### 5.2 티켓 필드

티켓은 최소한 다음 정보를 가진다.

- 프로젝트
- 프로젝트 내 고유 티켓 번호 및 표시 키
- 유형: Epic, Task, Subtask
- 제목
- 설명
- 상태
- 중요도
- 상위 티켓
- 생성자(creator)
- 담당자(handler/assignee, 미지정 가능)
- 생성일시
- 수정일시
- 마감일(due date, 선택)
- 계획 시작일 및 계획 종료일(간트차트용, 각각 선택; 마감일과 독립)
- 실제 최초 착수일시(선택), 진행률(0~100), 마일스톤 여부
- 완료일시(완료 상태 진입 시 기록 권장)
- 취소일시(취소 상태 진입 시 기록 권장)
- 정렬 및 보드 표시를 위한 순서 값

향후 필요 시 다음 필드를 추가할 수 있도록 확장 가능하게 설계한다.

- 예상 작업량 또는 스토리 포인트
- 라벨
- 컴포넌트
- 실제 완료일
- 스프린트

### 5.3 중요도

초기 중요도는 다음 5단계를 권장한다.

| 중요도 | 의미 |
|---|---|
| Trivial | 업무 영향이 거의 없는 개선 또는 정리 |
| Minor | 영향이 작고 우회가 가능한 문제 |
| Major | 일반적인 기본 중요도 |
| Critical | 주요 기능 또는 일정에 큰 영향이 있는 문제 |
| Blocker | 다른 업무 진행이나 서비스를 막는 최우선 문제 |

- 기본값은 `Major`로 한다.
- 내부 저장값은 변경에 강한 영문 코드로 유지하고, 화면 표시명은 다국어 처리 가능한 형태로 분리한다.

### 5.4 상태 및 유한 상태 기계

초기 상태는 다음과 같다.

- 등록
- 진행중
- 완료
- 보류
- 취소

상태 변경은 유한 상태 기계(Finite State Machine, FSM)로 제한한다. 초기 전이 규칙은 다음과 같이 제안한다.

| 현재 상태 | 이동 가능한 상태 |
|---|---|
| 등록 | 진행중, 보류, 취소 |
| 진행중 | 완료, 보류, 취소 |
| 보류 | 등록, 진행중, 취소 |
| 완료 | 진행중 |
| 취소 | 등록 |

- 완료 및 취소 상태는 업무 흐름상 종료 상태로 취급하되, 재개를 위한 명시적 역전이를 허용한다.
- 최초 진행 상태 진입 시 실제 최초 착수일시를 기록하고 이후 재개 시 덮어쓰지 않는다. 완료·취소 상태 진입 시 해당 현재 시각을 기록하고 그 상태에서 벗어나면 현재 시각 필드는 비우되 이전 값은 변경 이력에 보존한다.
- 상태 변경은 API에서도 반드시 검증하며 UI 제한만으로 처리하지 않는다.
- 상태 변경 시 변경자, 이전 상태, 새 상태, 변경일시를 이력에 기록한다.
- 향후 프로젝트별 워크플로 설정이 가능하도록 상태 및 전이 규칙을 도메인 서비스 또는 설정 계층으로 분리한다.

### 5.5 티켓 관계

초기 버전은 다음 관계를 지원한다.

- 관계 있음(Related)
- 의존함(Depends on)

요구사항은 다음과 같다.

- 하나의 티켓은 관계를 가지지 않거나 여러 관계를 가질 수 있다.
- 관계 수에는 별도 제한을 두지 않는다.
- 관계는 동일 프로젝트의 티켓 사이에서만 생성할 수 있다.
- `Related`는 대칭 관계로 취급한다.
- `Depends on`은 방향이 있는 관계로 취급한다. A와 B는 모두 티켓이며, 예를 들어 A가 B에 의존할 수 있다.
- 동일한 두 티켓 사이의 완전히 동일한 관계는 중복 생성하지 않는다.
- 티켓 자신과의 관계는 생성할 수 없다.
- 티켓 상세 화면에는 관계가 있는 티켓을 Jira와 유사한 작은 목록으로 표시한다.
- 관계 목록에는 관계 방향, 티켓 키, 제목 및 현재 상태를 표시하고 각 티켓의 상세 화면으로 이동할 수 있게 한다.
- A 화면에는 `B에 의존함`, B 화면에는 역방향 표현인 `A가 이 티켓에 의존함`으로 표시한다. 역방향 표현은 별도의 관계 유형으로 저장하지 않는다.
- A가 하나 이상의 티켓에 의존한다면 의존 대상이 모두 `완료` 상태여야만 A를 `완료` 상태로 변경할 수 있다.
- 의존 대상이 미완료이면 UI에서 원인을 안내하고 완료 동작을 비활성화하며, API와 칸반 드래그에서도 완료 전이를 거부한다.
- `Related` 관계는 상태 전이를 제한하지 않는다.

### 5.6 본문 및 댓글 포맷팅

- 티켓 설명과 댓글은 각각 독립된 Tiptap document이며 같은 versioned JSON 저장 계약과 viewer를 사용한다.
- 애플리케이션의 최초 rich-text 계약은 `body_schema_version=2`로 시작하고 Tiptap package 버전과 분리한다. package upgrade만으로 본문 계약 버전을 변경하지 않고 node·mark·attribute 구조가 바뀔 때만 새 계약과 converter를 추가한다.
- 최소 지원 서식은 제목, 굵게, 기울임, 밑줄, 취소선, 제한된 글자 크기·색상, 글머리 기호·번호 목록과 중첩 들여쓰기, 체크박스 목록, 인용, 코드, 링크 및 표이다. 글꼴 선택과 표 셀 배경색은 초기 필수 범위에서 제외한다.
- 저장 원본은 허용된 node·mark·attribute만 포함하는 Tiptap JSON이며 임의 HTML을 원본으로 저장하지 않는다. 서버는 JSON 구조·크기·깊이와 URL·내부 참조를 검증한다.
- 읽기 화면용 sanitized HTML과 검색·목록·보고서용 plain text는 JSON 원본에서 만든 파생 데이터로 취급한다. 렌더링 결과에도 allowlist sanitizer를 적용한다.
- Tiptap은 에디터와 문서 구조에만 사용한다. 티켓 댓글, 멘션, 첨부파일, optimistic locking, 변경 이력과 감사 로그는 애플리케이션 서비스가 직접 관리한다.
- 본문 일부에 다는 inline comment, Tiptap Cloud·협업·댓글·version history 서비스는 사용하지 않는다.
- Tiptap과 extension은 exact version과 lock file로 고정하고 build한 JavaScript·CSS bundle을 애플리케이션과 같은 서버에서 제공한다. 운영 runtime은 CDN, 외부 Tiptap 서비스, Node.js 또는 npm에 의존하지 않는다.

### 5.7 댓글

- 프로젝트 접근 권한이 있는 사용자는 권한 정책에 따라 댓글을 등록할 수 있다.
- 댓글은 작성자, 본문, 생성일시 및 수정일시를 가진다.
- 댓글 본문은 티켓 설명과 같은 Tiptap JSON 문서 계약·renderer·sanitizer를 사용한다.
- 프로젝트 사용자와 프로젝트 관리자는 프로젝트 안의 댓글을 작성자와 무관하게 수정하고 soft delete할 수 있다. 프로젝트 게스트는 댓글을 읽을 수 있지만 작성·수정·삭제할 수 없다.
- 삭제된 댓글의 감사 필요성을 고려하여 soft delete를 권장한다.
- 티켓 설명과 댓글에서 `@사용자` 형식의 멘션을 지원한다.
- 같은 프로젝트에 접근할 수 있는 사용자만 멘션 후보로 제공하고 서버에서도 대상 사용자의 프로젝트 접근 권한을 검증한다.
- 멘션이 생성되면 대상 사용자에게 미확인 멘션 항목을 생성한다.
- 멘션 수정 시 같은 사용자에게 중복 항목이 계속 생성되지 않도록 원본과 대상 사용자의 조합을 식별한다.
- 이메일, 실시간 push 및 toast 알림은 초기 범위에 포함하지 않는다.

### 5.8 첨부파일

- 티켓과 댓글에 파일을 첨부할 수 있다.
- 이미지 파일은 상세 뷰에서 미리보기로 표시한다.
- 본문 안의 이미지는 base64 또는 임의의 외부 URL을 저장하지 않고 권한 검사를 거치는 내부 attachment ID를 image node에서 참조한다.
- 이미지가 아닌 파일은 파일명, 크기 및 다운로드 링크를 표시한다.
- 파일명 충돌과 경로 조작을 방지하기 위해 내부 저장명은 UUID 등으로 생성한다.
- 원본 파일명, MIME type, 크기, 저장 키, 업로더 및 업로드 일시는 DB에 기록한다.
- 허용 확장자, MIME type 및 최대 파일 크기는 환경 설정으로 제한한다.
- 파일 제공 시 프로젝트 접근 권한을 검사해야 하며, 마운트 디렉터리를 웹 서버의 공개 정적 경로로 직접 노출하지 않는다.
- 프로젝트 게스트는 첨부파일을 조회·다운로드할 수 있지만 업로드·삭제할 수 없다. 프로젝트 사용자와 프로젝트 관리자는 프로젝트 안의 첨부파일을 업로더와 무관하게 등록·삭제할 수 있다.

### 5.9 변경 이력

Jira와 유사한 추적성을 위해 다음 변경을 티켓 활동 이력으로 저장하는 것을 권장한다.

- 상태 변경
- 담당자 변경
- 중요도 변경
- 상위 티켓 변경
- 제목 및 설명 변경
- 마감일 변경
- 티켓 관계 추가 및 삭제
- 첨부파일 추가 및 삭제

각 이력에는 변경자, 변경 시각, 변경 항목, 이전 값 및 새 값을 기록한다.

- 기간별 보고서의 근거로 사용하도록 티켓 생성과 주요 변경 이력 기록을 필수로 한다. 일정·진행률·삭제·복구도 포함한다.
- 한 티켓 변경은 하나의 증가하는 티켓 버전과 변경 전후 스냅샷을 가진 이벤트로 기록하며, 티켓 저장과 같은 트랜잭션에서 처리한다.
- 본문 변경 이력은 Tiptap의 브라우저 undo stack이나 유료 version history 기능에 의존하지 않고 명시적인 서버 저장 단위로 직접 기록한다. 키 입력마다 이력을 생성하지 않는다.
- 티켓 활동 이력은 30일 보존 감사 로그와 별개로 티켓이 영구 삭제될 때까지 유지한다.
- 이벤트와 스냅샷은 버전을 명시한 구조로 저장하고, 보고서 생성 시점의 현재 상태만으로 과거 기간의 변경을 추정하지 않는다.

### 5.10 티켓 삭제 및 휴지통

- 티켓 삭제는 즉시 물리 삭제하지 않고 프로젝트 휴지통으로 이동하는 soft delete 방식으로 처리한다.
- 프로젝트 사용자와 프로젝트 관리자는 프로젝트 안의 티켓을 creator·assignee와 무관하게 휴지통으로 이동하거나 복구할 수 있다. 프로젝트 게스트는 휴지통 쓰기를 수행할 수 없다.
- 휴지통으로 이동한 티켓은 일반 목록, 칸반 보드 및 일반 사용자의 직접 접근에서 제외하고 수정할 수 없게 한다.
- Epic 또는 Task를 휴지통으로 이동하면 그 하위 Task와 Subtask도 하나의 단위로 함께 이동하며, 복구할 때도 함께 복구한다.
- 다른 활성 티켓이 삭제 대상 티켓에 `Depends on` 관계를 가지고 있으면 관계를 먼저 해제하거나 해당 티켓을 완료하기 전까지 휴지통 이동을 거부한다.
- 휴지통 화면에는 티켓 키, 제목, 유형, 삭제자, 삭제일시 및 영구 삭제 예정일을 표시한다.
- 휴지통에 들어간 지 30일이 지난 티켓은 정기 정리 작업을 통해 자동으로 영구 삭제한다.
- 영구 삭제 시 하위 티켓, 댓글, 관계, 활동 이력, 멘션 및 실제 첨부파일도 함께 정리한다. 시스템 관리자가 보존·정리 정책과 수동 실행을 관리하며 자동 실행은 내부 system actor로 기록한다.
- 삭제 후 복구하거나 영구 삭제해도 이미 발급한 티켓 번호는 다시 사용하지 않는다.

## 6. 내 작업 대시보드

- 로그인에 성공하면 내 작업 대시보드를 기본 화면으로 표시한다.
- `내 티켓`은 현재 담당자가 나인 티켓과 담당자가 없으면서 내가 생성한 티켓의 합집합이다. 내가 생성했더라도 다른 담당자가 지정된 티켓은 내 티켓에 포함하지 않으며 생성자 기준 조회는 티켓 목록 필터로 제공한다.
- 대시보드에는 다음 정보를 요약 카드와 짧은 티켓 목록으로 제공한다.
  - 내 미완료 티켓
  - 마감일이 지난 내 미완료 티켓
  - 오늘부터 이번 주 일요일까지 마감되는 내 미완료 티켓
  - 최근 수정된 내 티켓
- 각 요약의 티켓 수를 표시하고, 항목을 선택하면 해당 조건이 적용된 전체 티켓 목록으로 이동한다.
- 사용자가 접근 가능한 프로젝트의 티켓만 집계하고 표시한다.
- 시스템 관리자도 대시보드에서는 명시적으로 참여한 프로젝트만 집계하며 전체 프로젝트 override를 적용하지 않는다.
- 대시보드에 `미확인 멘션` 영역을 제공한다.
- 미확인 멘션에는 멘션한 사용자, 프로젝트, 티켓 키, 댓글 또는 설명의 일부 및 멘션 시각을 표시한다.
- 멘션을 선택하면 해당 티켓과 댓글 위치로 이동하며 확인 상태로 변경한다.
- 사용자는 개별 멘션 또는 모든 멘션을 확인 상태로 변경할 수 있다.
- 접근 권한을 잃은 프로젝트의 멘션은 내용이나 티켓 존재 여부를 노출하지 않는다.
- 대시보드 정보는 페이지 진입 및 수동 새로고침 시 갱신하며 실시간 push, toast 및 이메일 알림은 제공하지 않는다.

## 7. 티켓 목록

### 7.1 기본 동작

- Jira의 티켓 목록과 유사한 표 또는 목록 형태로 표시한다.
- 항목을 클릭하면 현재 목록 화면 안에서 상세 뷰어를 인라인 패널로 표시한다.
- `크게 보기`를 선택하면 독립된 티켓 상세 페이지를 연다.
- URL로 특정 티켓 상세 페이지에 직접 접근할 수 있어야 한다.
- 사용자가 접근할 수 없는 프로젝트 또는 티켓은 존재 여부도 불필요하게 노출하지 않는다.

### 7.2 필터 및 정렬

다음 항목으로 필터할 수 있다.

- 티켓 유형
- 상태
- 중요도
- Epic
- 상위 Task
- 생성일 및 수정일 범위
- 마감일 범위
- 생성자
- 담당자
- 담당자 미지정 여부

다음 항목을 기준으로 정렬할 수 있도록 권장한다.

- 생성일
- 수정일
- 마감일
- 중요도
- 티켓 번호

- 여러 필터를 동시에 적용할 수 있어야 한다.
- 필터 상태는 URL query parameter로 표현하여 새로고침 및 링크 공유가 가능하도록 권장한다.

### 7.3 페이지네이션

- 서버 사이드 페이지네이션을 지원한다.
- 페이지 크기는 사용자가 10개, 20개, 50개 중 선택할 수 있다.
- 기본 페이지 크기는 20개를 권장한다.
- 안정적인 페이지 결과를 위해 항상 명시적인 보조 정렬 기준을 적용한다.

### 7.4 텍스트 검색 확장 계획

- 초기 MVP에 티켓 키와 제목을 대상으로 대소문자를 구분하지 않는 단순 텍스트 검색을 포함한다.
- 향후 제목, 설명 및 댓글을 대상으로 한국어 전문 검색을 지원한다.
- 검색 로직은 저장소 계층 또는 검색 서비스 인터페이스 뒤에 분리하여 DB 쿼리와 직접 결합하지 않는다.
- SQLite FTS만으로 한국어 형태소 검색 품질이 부족할 수 있으므로 규모와 품질 요구가 생기는 시점에 PostgreSQL 전문 검색, PGroonga, OpenSearch/Elasticsearch 등의 도입을 검토한다.
- 검색 인덱스는 원본 데이터가 아니라 재생성 가능한 파생 데이터로 취급한다.
- 모든 검색 결과에는 프로젝트 접근 권한 필터가 적용되어야 한다.

### 7.5 저장된 필터

- 사용자는 현재 선택한 필터, 정렬 기준 및 표시 개수를 이름을 붙여 개인 필터로 저장할 수 있다.
- 프로젝트 관리자는 프로젝트 구성원이 사용할 수 있는 공유 필터를 생성, 수정 및 삭제할 수 있다.
- 저장된 필터는 티켓 목록과 호환되는 범위에서 칸반 보드에서도 불러올 수 있다.
- 초기 버전에는 JQL과 같은 사용자 입력형 쿼리 언어를 도입하지 않는다.
- 저장 데이터는 허용된 필드, 연산자, 값, 정렬 및 페이지 크기로 구성된 버전이 명시된 JSON 구조를 사용한다.
- 저장된 JSON은 실행 가능한 SQL이나 코드가 아니며, 불러올 때 서버가 필드, 타입, 프로젝트 권한을 다시 검증한 뒤 기존 목록 조회 조건으로 변환한다.
- 저장된 필터의 이름 변경, 덮어쓰기 및 삭제를 지원한다.

## 8. 칸반 보드

### 8.1 기본 구성

- 칸반 보드는 프로젝트에 종속된다.
- 상태별 열에 Task와 Subtask를 카드 형태로 표시한다.
- Epic은 카드의 상위 묶음으로 표현하며 Epic 단위로 접거나 펼칠 수 있다.
- Epic이 없는 Task를 위한 별도 `Epic 없음` 묶음을 제공한다.
- 카드를 클릭하면 티켓 상세 뷰어 또는 상세 페이지로 이동한다.

### 8.2 계층 표시 규칙

- Task와 그 Subtask가 같은 상태인 경우 Subtask를 해당 Task 아래에 달린 형태로 표시한다.
- Subtask가 상위 Task와 다른 상태에 있는 경우 해당 Subtask를 자신의 상태 열에 표시한다.
- 다른 상태 열에 표시된 동일 상위 Task의 Subtask들은 상위 Task를 식별할 수 있는 하나의 그룹으로 묶는다.
- 카드에는 최소한 티켓 키, 제목, 중요도, 담당자 및 마감일을 표시한다.

### 8.3 드래그를 통한 상태 변경

- 사용자는 카드를 드래그하여 다른 상태 열로 이동할 수 있다.
- FSM에서 허용한 상태로만 드롭할 수 있다.
- 허용되지 않은 대상은 UI에서 명확히 표시하고 드롭할 수 없게 한다.
- 서버는 드래그 요청에 대해서도 현재 상태와 권한 및 FSM 전이를 다시 검증한다.
- 미완료 티켓에 의존하는 카드의 `완료` 상태 이동은 드롭할 수 없게 표시하고 서버에서도 거부한다.
- 동시 수정 충돌 시 사용자에게 갱신 필요를 알리고 보드를 새 데이터로 동기화한다.

### 8.4 필터

티켓 목록과 동일한 주요 필터를 지원한다.

- 유형
- 상태
- 중요도
- Epic
- 생성자
- 담당자
- 날짜 범위
- 향후 Sprint

## 9. Sprint 확장 계획

Sprint는 초기 MVP 범위에서는 제외하되, 다음 기능을 추가할 수 있도록 데이터 모델과 서비스 경계를 준비한다.

- 프로젝트별 Sprint 생성
- 시작일 및 종료일 지정
- 계획, 진행중, 종료 상태 관리
- Sprint 시작 및 종료 동작
- 다수의 미완료 Ticket 및 Epic 일괄 등록
- Sprint 종료 시 미완료 티켓을 백로그 또는 다음 Sprint로 일괄 이동
- 티켓 목록 및 칸반 보드에서 Sprint 필터 제공
- Sprint별 완료율 및 기본 통계 제공

Sprint와 Ticket 사이의 관계는 향후 이력 요구를 고려한다. 티켓의 현재 Sprint만 필요한지, 여러 Sprint에 걸친 이동 이력을 보존할지 구현 전에 확정한다.

### 9.1 간트차트 확장 계획

- 후속 기능으로 프로젝트별 간트차트를 제공한다. 화면과 자동 일정 계산은 후속 범위이며 저장 구조는 MVP 기반 작업에서 준비한다.
- 계획 시작일·종료일, 마감일, 실제 최초 착수일시, 완료일시, 진행률 및 마일스톤 여부를 구분한다.
- 일정 미정 티켓도 허용하며 시작일과 종료일이 모두 있으면 시작일이 종료일보다 늦을 수 없다.
- Epic·Task·Subtask 계층과 동일 프로젝트의 의존 관계를 사용한다.
- 의존 관계에는 일정 연결 유형(FS·SS·FF·SF)과 달력 날짜 기준 지연 일수를 저장할 수 있다. 초기 업무 의존 관계는 FS·0일을 사용한다.
- 일정 연결 유형과 무관하게 MVP의 의존 대상 완료 검사 정책은 유지한다.
- 영업일 달력, 자동 재계획, 기준 일정 비교와 일정 편집 UI는 후속 구현 전에 결정한다.

### 9.2 로컬 LLM 기반 기간별 Markdown 보고서 확장 계획

- 후속 기능으로 사용자가 기간과 접근 가능한 프로젝트를 선택해 주간보고 등의 Markdown 보고서 생성을 요청할 수 있다.
- 데이터 구조는 MVP 기반 작업에서 준비하며 로컬 LLM 연결, 작업 실행기와 보고서 UI는 후속 범위다.
- 기간은 사용자의 시간대에서 결정한 시작·종료 경계를 UTC의 `[시작, 종료)` 구간으로 저장한다.
- 입력에는 기간 시작·종료 경계의 티켓 상태와 기간 내 변경 이벤트를 포함한다. 동일 티켓의 완료·재개 등 여러 변경도 모두 보존한다.
- 보고서 생성에 사용한 선택 조건, 근거 데이터 스냅샷, 수집 시각, 스키마 버전과 해시를 저장한다.
- 원본 삭제나 이력 부족 등으로 근거가 불완전하면 범위 제한을 명시하며 완전한 보고서로 표현하지 않는다.
- 스킬은 재사용 가능한 보고서 작성 지침이며 이름·설명·Markdown 지침·입출력 계약·생성 기본값을 버전별로 보관한다. 실행 가능한 코드나 도구 권한을 포함하지 않는다.
- 요청은 특정 스킬 버전에 고정한다. 요청별 실행 시도를 구분하여 로컬 모델 식별자·가능한 모델 digest·생성 옵션·실제 입력·원응답·검증된 Markdown 결과·근거 참조·실패 코드 및 실행 시간을 기록한다.
- 본문과 댓글은 신뢰하지 않는 근거 데이터로 취급하며 스킬의 지시문과 구분한다. 출력은 지정된 구조와 입력 근거 참조를 검증하고 Markdown viewer에서 정화한다.
- 생성 요청, 데이터 수집, 실행 및 결과 조회 시 프로젝트 권한을 검사한다. 여러 프로젝트를 포함한 결과는 모든 원본 프로젝트에 대한 현재 접근 권한을 요구한다.
- 보고서용 근거 스냅샷은 원본 티켓 영구 삭제 후에도 기존 보고서 재검토를 위해 보고서와 함께 남는다. 보고서 자체의 삭제 시 입력·시도·출력도 함께 정리한다.
- 비밀값과 인증 헤더는 실행 기록에 저장하지 않는다. LLM 연결 주소와 자격증명은 외부 설정으로 분리하고 사용자 입력으로 임의의 주소를 실행하지 않는다.
- 보고서 자동 발송, 티켓 자동 수정 및 임의의 코드 실행은 이 확장 범위에 포함하지 않는다.
- 보고서 보존 기간, 입력량 제한, 실행기와 로컬 모델 제공자는 실제 기능 구현 전에 확정한다.

## 10. 데이터 및 애플리케이션 설계 지침

### 10.1 계층 분리

권장 계층은 다음과 같다.

```text
API / Web UI
    ↓
Application Service
    ↓
Domain / Permission / FSM
    ↓
Repository Interface
    ↓
SQLAlchemy ORM / File Storage / Search Adapter
```

- API 계층에서 직접 ORM 쿼리를 작성하지 않는다.
- 프로젝트 권한, 티켓 계층 규칙 및 FSM 검증은 재사용 가능한 서비스로 구현한다.
- DB별 차이가 필요한 기능은 저장소 또는 adapter 안에 격리한다.
- SQLite에서는 외래 키 검사를 명시적으로 활성화한다.
- 스키마 변경은 Alembic과 같은 migration 도구로 관리한다.
- 날짜와 시간은 DB에 UTC로 저장하고 화면에서 사용자 또는 시스템 시간대로 변환한다.

### 10.2 동시성 및 무결성

- 프로젝트 키 및 프로젝트 내 티켓 번호에는 고유 제약 조건을 적용한다.
- 티켓 관계, 프로젝트 구성원 등 중복되면 안 되는 데이터에는 DB 제약 조건을 함께 둔다.
- 티켓 수정 충돌 방지를 위해 버전 번호 또는 수정일시 기반 optimistic locking을 권장한다.
- 상위 티켓 변경, 상태 변경 및 관련 이력 기록은 하나의 트랜잭션으로 처리한다.

### 10.3 API 및 오류 처리

- UI와 분리된 API 구조를 권장한다.
- 모든 쓰기 요청은 인증, 프로젝트 권한, 입력값 및 도메인 규칙을 서버에서 검증한다.
- 오류 응답은 사용자 메시지와 개발용 오류 코드를 구분한다.
- 목록 API는 필터, 정렬, 페이지 및 페이지 크기를 명시적으로 입력받는다.

## 11. 보안 및 운영 요구사항

- 비밀번호 및 비밀 키를 코드나 이미지에 포함하지 않고 환경 변수 또는 secret으로 주입한다.
- 세션 쿠키 사용 시 `HttpOnly`, `Secure`, 적절한 `SameSite` 설정을 적용한다.
- 입력값 검증, CSRF 방어, XSS 방어 및 SQL injection 방어를 적용한다.
- ORM 사용 여부와 무관하게 동적 SQL 문자열 결합을 피한다.
- 첨부파일 업로드 시 파일 크기, MIME type 및 저장 경로를 검증한다.
- 로그인 실패 횟수 제한 또는 지연 정책을 권장한다.
- 사용자, 권한, 프로젝트 구성원 및 티켓 주요 변경에 대한 감사 로그를 반드시 남긴다.
- 감사 로그의 기본 보존 기간은 30일이며, 보존 기간이 지난 로그는 정기 정리 작업으로 영구 삭제한다.
- 감사 로그 보존 일수와 정리 작업 실행 주기는 외부 설정 파일 또는 환경 변수로 조정할 수 있어야 한다.
- 기본 정리 작업은 24시간마다 실행하며, 재시작이나 중단 후에도 중복 삭제 또는 누락 없이 멱등적으로 동작한다.
- 컨테이너 상태 확인을 위한 health check endpoint를 제공한다.
- SQLite DB 및 첨부파일을 일관된 시점에 백업하고 복구 절차를 문서화한다.

### 11.1 백업 아카이브 다운로드(후속 기능)

- 시스템 관리자는 관리 화면에서 현재 영속 데이터를 하나의 압축 파일로 생성하여 다운로드할 수 있다.
- 실행 중인 SQLite 파일을 그대로 압축하지 않고 SQLite backup API 등을 이용해 일관된 DB 스냅샷을 먼저 만든다.
- 생성된 아카이브에는 DB 스냅샷, 첨부파일 및 백업 시각과 애플리케이션 버전을 담은 manifest를 포함한다.
- 기존 `backups` 디렉터리와 생성 중인 임시 파일은 아카이브에서 제외하여 백업 파일이 재귀적으로 포함되지 않게 한다.
- 백업 생성과 다운로드는 시스템 관리자만 수행할 수 있으며 작업 시작, 성공, 실패 및 다운로드 이력을 감사 로그에 기록한다.
- 다운로드용 파일은 설정된 보관 기간이 지나면 자동으로 삭제한다.
- 다른 DB로 전환할 가능성을 고려하여 DB 스냅샷 생성은 DB별 backup adapter로 분리한다.
- 초기 복구는 컨테이너를 중지한 상태에서 아카이브를 마운트 디렉터리에 복원하는 운영 절차로 제공하고, 웹 화면의 자동 복구는 별도 후속 기능으로 둔다.

### 11.2 UI 지원 범위

- 회사 정책상 모바일 접속을 지원하지 않으므로 모바일 전용 화면과 반응형 모바일 최적화는 현재 범위에서 제외한다.
- 다크 모드는 초기 범위에서 제외한다.
- 추후 테마 추가 비용을 낮추기 위해 화면 색상을 개별 컴포넌트에 하드코딩하지 않고 공통 CSS 변수 또는 디자인 토큰으로 관리한다.

## 12. 초기 MVP 범위

### 12.1 포함

- 로그인 및 로그아웃
- 최초 기동 시 초기 시스템 관리자 생성 및 비밀번호 초기화
- 일반 사용자 및 시스템 관리자 관리
- 계층형 조직 관리
- 조직 체계 JSON 가져오기 및 내보내기
- 프로젝트 생성 및 구성원/역할 관리
- Epic, Task, Subtask 생성·조회·수정 및 계층 이동
- 티켓 관계(Related, Depends on) 목록 표시 및 의존성 기반 완료 차단
- Tiptap JSON 기반 구조화 설명 및 댓글
- 사용자 멘션 및 대시보드의 미확인 멘션 확인
- 첨부파일 업로드·다운로드 및 이미지 표시
- FSM 기반 상태 변경
- 중요도, 생성자, 담당자, 날짜 관리
- 티켓 휴지통, 복구 및 30일 후 자동 영구 삭제
- 내 작업 대시보드
- 티켓 목록, 필터, 정렬 및 페이지네이션
- 티켓 키와 제목 대상 단순 텍스트 검색
- JQL 없는 개인·공유 저장 필터
- 인라인 상세 뷰 및 전체 상세 페이지
- Epic 그룹 기반 칸반 보드
- 드래그를 통한 상태 변경
- Docker 실행 및 volume mount 기반 영속화
- Alembic 기반 DB migration

### 12.2 후속 범위

- 간트차트와 일정 편집
- 로컬 LLM 기반 기간별 Markdown 보고서 생성과 보고서 스킬 관리
- Sprint 및 백로그
- 한국어 전문 검색
- 관리자용 백업 아카이브 생성 및 다운로드
- 사용자 정의 상태와 워크플로
- 사용자 정의 필드
- 프로젝트 및 업무 통계
- 외부 인증(SSO, OAuth/OIDC, LDAP)
- 오브젝트 스토리지 기반 첨부파일 저장
- 필요성이 생길 경우 다크 모드

### 12.3 명시적 제외 범위

- 이메일 알림
- 실시간 push 및 toast 알림
- JQL과 같은 사용자 입력형 쿼리 언어
- 티켓 일괄 수정
- 티켓 복제
- 프로젝트 및 티켓 CSV 가져오기·내보내기
- 사용자 그룹 또는 팀 기반 권한 일괄 부여
- 모바일 접속 및 모바일 전용 UI

## 13. 확정된 구현 정책

1. 프로젝트 게스트는 프로젝트 업무 데이터를 읽기만 하며 담당자가 될 수 없다. 프로젝트 사용자는 creator·assignee와 무관하게 프로젝트 내 모든 티켓과 티켓 종속 콘텐츠를 생성·수정·상태 전이·휴지통 이동·복구할 수 있다. 프로젝트 관리자는 이에 더해 프로젝트와 참여자·역할을 관리하며, 시스템 관리자의 전체 프로젝트 override는 감사 로그에 남긴다.
2. 프로젝트 사용자와 프로젝트 관리자는 작성자·업로더와 무관하게 프로젝트 내 댓글과 첨부파일을 관리한다. 댓글은 soft delete하고, 첨부파일은 즉시 접근을 차단한 뒤 기본 30일 후 실제 파일을 영구 삭제한다. 이 보존 기간은 외부 설정으로 변경할 수 있다.
3. 완료 또는 취소 티켓의 제목, 설명, 중요도, 담당자, 상위 티켓 및 관계 등 업무 필드는 수정할 수 없다. 수정이 필요하면 FSM에 따라 먼저 재개해야 하며, 사후 기록을 위한 댓글과 첨부파일 추가는 허용한다.
4. Epic과 하위 Task의 상태는 자동으로 연동하지 않는다. Epic 완료 시 미완료 Task가 있으면 사용자의 명시적 확인을 요구하되 별도 사유는 입력받지 않으며, 확인 후에는 완료 전이를 차단하지 않는다.
5. Task를 같은 프로젝트의 다른 Epic으로 이동하거나 Epic에서 분리해도 Subtask는 해당 Task 아래에 그대로 유지한다. Subtask의 상태, 담당자 및 순서는 변경하지 않으며 다른 프로젝트로의 이동은 허용하지 않는다.
6. 조직 소속은 프로젝트 접근 권한을 자동으로 부여하거나 회수하지 않는다. 프로젝트 접근은 `ProjectMember`에 명시적으로 등록된 사용자에게만 허용한다.
7. 첨부파일은 기본 25MB까지 허용하고 확장자와 MIME type을 모두 검증하며 실행 파일과 스크립트 파일을 차단한다. 파일별 최대 크기, 전체 저장 용량, 허용 확장자와 MIME type은 외부 설정으로 조정할 수 있게 하며 운영 값은 첨부파일 기능 구현 시 최종 확인한다.
8. 초기 인증은 서버 측 세션 방식을 사용하고 브라우저에는 opaque 세션 토큰만 쿠키로 전달한다. JWT는 외부 API나 별도 클라이언트가 필요해질 때 검토한다.
9. 감사 로그의 기본 보존 기간은 30일이며 기간이 지난 로그는 영구 삭제한다. 보존 일수와 정리 작업 실행 주기는 외부 설정 파일 또는 환경 변수로 조정할 수 있게 한다.
10. 초기 MVP에 티켓 키와 제목을 대상으로 하는 단순 텍스트 검색을 포함한다. 설명과 댓글의 한국어 전문 검색은 후속 범위로 둔다.
11. 티켓 업무 필드 편집 요청의 값이 현재 값과 모두 같으면 성공으로 처리하되 버전, 변경 이력과 감사 로그를 추가하지 않는다. `expected_version` 충돌 검사는 no-op 판정보다 먼저 수행한다.
12. 티켓 설명과 댓글은 Tiptap 기반의 versioned JSON document로 저장한다. Tiptap은 에디터와 문서 구조에만 사용하고 댓글·멘션·첨부파일·변경 이력은 서버에서 직접 관리한다. editor bundle은 exact version으로 고정해 애플리케이션 서버가 제공하며 inline comment와 외부 Tiptap 서비스는 사용하지 않는다.

## 14. 용어

| 용어 | 의미 |
|---|---|
| 시스템 관리자 | 전체 사용자, 조직 및 시스템을 관리하는 사용자 |
| 프로젝트 관리자 | 프로젝트 사용자의 권한에 더해 특정 프로젝트와 참여자·역할을 관리하는 사용자 |
| 프로젝트 사용자 | 특정 프로젝트의 모든 티켓 업무를 수행하는 일반 참여자 |
| 프로젝트 게스트 | 특정 프로젝트의 티켓과 종속 콘텐츠를 읽기만 하는 참여자 |
| Creator | 티켓을 최초 등록한 사용자 |
| Handler / Assignee | 티켓을 현재 담당하는 사용자 |
| Parent Ticket | Task의 Epic 또는 Subtask의 Task |
| FSM | 허용된 상태 이동만 수행하도록 제한하는 유한 상태 기계 |
