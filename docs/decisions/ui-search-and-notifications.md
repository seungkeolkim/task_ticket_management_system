# UI, search, and notification decisions

| ID | 날짜 | 상태 | 결정 | 근거·영향 |
|---|---|---|---|---|
| UI-001 | 2026-09-16 | DECIDED | 초기 MVP 검색은 접근 가능한 티켓의 키와 제목만 대상으로 대소문자를 구분하지 않는 단순 검색을 제공한다. | 전문 검색 인프라 없이 자주 쓰는 탐색을 제공한다. 설명·댓글 한국어 전문 검색은 후속 범위다. |
| UI-002 | 2026-09-16 | DECIDED | 저장 필터는 허용된 필드·연산자·값·정렬·페이지 크기의 versioned JSON으로 저장하며 JQL 같은 실행형 사용자 query 언어는 제공하지 않는다. | 입력을 SQL이나 코드로 해석하지 않고 기존 typed filter DTO로 검증할 수 있다. |
| UI-003 | 2026-09-16 | DECIDED | 목록 페이지 크기는 10·20·50만 허용하고 기본값은 20이며 항상 안정적인 보조 정렬을 적용한다. | 페이지 이동 중 중복·누락을 줄이고 임의의 과도한 조회를 막는다. |
| UI-004 | 2026-09-16 | DECIDED | 칸반은 프로젝트별 상태 열과 Epic group을 사용하고 Task·Subtask가 다른 상태일 때 Subtask를 자신의 상태 열에 표시한다. | 실제 상태를 숨기지 않으면서 부모 업무를 식별할 수 있게 한다. |
| UI-005 | 2026-09-16 | DECIDED | 대시보드와 멘션은 페이지 진입 및 수동 새로고침으로 갱신하며 MVP에 email, realtime push, toast를 포함하지 않는다. | 초기 범위에서 메시징 인프라와 전달 실패 정책을 제외한다. |
| UI-006 | 2026-09-16 | DECIDED | 회사 정책에 따라 모바일 화면과 모바일 최적화는 지원하지 않고 초기 범위에서 dark mode도 제외한다. 색상은 향후 변경을 위해 design token으로 관리한다. | 데스크톱 사내 업무 흐름에 집중하면서 테마 추가 비용은 낮춘다. |
| UI-007 | 2026-09-16 | DECIDED | 초기 MVP의 화면 책임은 아래 화면 구성 목록을 기준으로 한다. | 구현 전에 필요한 사용자 진입점을 빠뜨리지 않기 위한 범위 기록이며, 화면 배치·컴포넌트·상호작용 방식은 고정하지 않는다. |
| UI-008 | 2026-09-16 | SUPERSEDED | UI 목업 단계에서는 `/`에서 인증 없이 내 작업 대시보드를 제공하고, 화면별 GET route와 메모리 내 표시용 데이터로 화면 간 탐색을 지원한다. 쓰기 동작은 구현하지 않는다. | 인증·도메인 API보다 먼저 전체 정보 구조와 연결 흐름을 검토하기 위한 임시 실행 방식이다. 실제 기능 구현 시 화면 책임은 유지하되 인증 정책과 데이터 공급부를 application service로 교체한다. |
| UI-009 | 2026-09-17 | DECIDED | UI-008을 대체한다. `/`는 대시보드 주소로 유지하고 미인증 접근은 로그인으로, 초기 비밀번호 변경 대상은 변경 화면으로 이동한다. 로그인 후 안전한 원래 내부 주소로 복귀한다. | 기존 목업의 탐색 구조를 유지하면서 인증 화면부터 실제 서비스로 연결한다. 시스템 관리 화면은 관리자 역할을 검사하고 업무 화면의 예시 데이터는 후속 화면별 작업에서 교체한다. |
| UI-010 | 2026-09-18 | DECIDED | 사용자·조직 관리 화면의 목록과 생성 form을 실제 서비스에 연결한다. 사용자 목록은 검색과 10·20·50 페이지 크기, 조직 목록은 전체 트리와 직속 인원 수를 제공한다. | 기존 Jinja2 레이아웃을 유지하고 준비되지 않은 편집·초기화·JSON 버튼과 고정 통계를 제거한다. 생성 후 redirect로 목록을 갱신하며 입력 오류 시 비밀번호 외의 값을 보존한다. |

## UI-007 초기 MVP 화면 구성 목록

이 목록은 필요한 화면과 각 화면이 담당할 기능 범위만 정의한다. URL 구조, 메뉴 배치, 레이아웃, 탭·팝업·패널 사용 여부와 같은 상세 UI는 구현 시점에 변경할 수 있다. 관련 기능을 한 화면에 합치거나 여러 화면으로 나누더라도 아래 책임에 접근할 수 있어야 한다.

### 인증 및 공통

- 로그인
- 비밀번호 변경: 최초 로그인 또는 관리자 초기화 후 강제 변경과 사용자의 일반 변경
- 내 작업 대시보드: 내 담당·생성 티켓, 기한 정보, 최근 수정 티켓 및 미확인 멘션
- 내 프로젝트: 사용자가 접근할 수 있는 프로젝트 조회 및 선택

### 프로젝트 업무

- 티켓 목록·검색: 필터, 정렬, 페이지네이션 및 저장 필터 사용
- 티켓 상세: 목록 내 인라인 상세와 직접 접근 가능한 전체 상세를 모두 제공
- 티켓 생성·편집
- 칸반 보드

### 프로젝트 관리

- 프로젝트 기본 정보 관리
- 프로젝트 구성원 및 역할 관리
- 프로젝트 휴지통과 티켓 복구

### 시스템 관리

- 사용자 관리와 비밀번호 초기화
- 조직 계층 관리와 조직 JSON 가져오기·내보내기 및 적용 전 미리보기
- 전체 프로젝트 생성·조회·수정 및 관리 대상 프로젝트 접근

댓글, 멘션, 첨부파일, 티켓 관계와 변경 이력은 티켓 상세 책임에 포함한다. 개인·공유 저장 필터 관리는 티켓 목록과 칸반의 필터 책임에 포함하며, 현재 단계에서는 각각을 독립 화면으로 요구하지 않는다.
| UI-011 | 2026-09-19 | DECIDED | 프로젝트 생성·기본 조회·참여자 추가와 내 프로젝트를 실제 DB에 연결한다. 목록은 이름·ID 순서와 검색, 10·20·50 페이지 크기를 사용한다. 프로젝트 상세의 진입점은 기본 정보이며 프로젝트 하위의 미구현 업무 경로는 권한 검사 후 준비 중 안내를 표시한다. | 실제 프로젝트 키와 기존 목업 키가 같아도 예시 티켓·구성원을 실제 업무처럼 노출하지 않는다. 대시보드의 예시 데이터는 다음 연결 순서까지 유지한다. |
| UI-012 | 2026-09-22 | DECIDED | 프로젝트 티켓의 첫 연결은 생성·키/제목 검색 목록·인라인 조회·직접 상세와 10·20·50 페이지 크기를 실제 DB에 연결한다. 설명은 Markdown 원문을 안전한 일반 텍스트로 표시하고 준비되지 않은 편집·댓글·관계·첨부·이력 조회 동작은 실제 데이터처럼 노출하지 않는다. | 티켓 생성 이력은 즉시 저장하되 Markdown renderer와 상세 확장 기능은 각 보안·권한 규칙을 구현할 때 연결한다. 목록은 수정일시·ID 역순의 안정 정렬을 사용한다. |
| UI-013 | 2026-09-22 | DECIDED | 내 티켓은 `assignee = 현재 사용자` 또는 `assignee IS NULL AND creator = 현재 사용자`로 정의한다. 대시보드는 이 범위의 미완료·기한 초과·이번 주 마감·최근 수정 티켓을 명시적 프로젝트 membership 안에서만 집계하고, 생성자 단독 조건은 전역 티켓 목록 필터로 제공한다. 칸반 첫 연결은 실제 DB의 상태 열과 Epic·Task·Subtask 그룹을 읽기 전용으로 표시한다. | 담당자가 지정된 뒤에도 생성자라는 이유만으로 개인 작업에 남는 혼동을 막는다. 시스템 관리자 대시보드는 내 프로젝트 의미를 유지해 override하지 않고, 특정 비참여 프로젝트 칸반 직접 접근만 감사되는 override를 사용한다. 멘션 읽음 처리와 칸반 상태 변경은 후속 쓰기 범위다. |
| UI-014 | 2026-09-23 | DECIDED | 칸반 상태 이동은 외부 drag library 없이 브라우저 기본 drag-and-drop과 키보드로 사용할 수 있는 상태 선택 control을 함께 제공한다. FSM·권한·의존성·버전 정보로 허용 대상을 표시하되 서버 전이 API를 최종 기준으로 삼고, 성공 또는 stale 충돌 뒤에는 보드를 다시 읽어 계층 표시를 동기화한다. 같은 상태 안의 순서 변경과 `sort_order` 편집은 별도 범위로 둔다. | 별도 frontend build 의존성을 추가하지 않고 접근 가능한 대체 조작을 제공하며, Task·Subtask 상태에 따라 중첩 위치가 달라지는 화면을 클라이언트에서 중복 계산하지 않는다. |

## Open decisions

- 목록의 인라인 상세 패널을 server-rendered partial, HTMX, 또는 별도 client state 중 무엇으로 구현할지는 UI 기반 작업 전에 확정한다.
