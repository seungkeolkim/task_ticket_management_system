name: CI

on:
  push:
  pull_request:

jobs:
  powershell-runner:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: python -m pip install -e ".[dev]"
      - name: Verify PowerShell Compose runner without starting Docker
        run: python -m pytest tests/unit/test_compose_runner.py tests/unit/test_powershell_compose_runner.py

  test:
    runs-on: ubuntu-latest
    env:
      APP_DATA_ROOT: ./.ci-data
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install dependencies
        run: python -m pip install -e ".[dev]"
      - name: Lint
        run: ruff check .
      - name: Test
        run: pytest
      - name: Verify migrations
        run: |
          alembic upgrade head
          alembic check
          alembic downgrade base
